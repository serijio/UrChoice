<!-- romboide.xml -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="@color/blue"/> <!-- Cambia este color si lo deseas -->
    <size android:width="100dp" android:height="200dp"/> <!-- Cambia estas dimensiones según tus necesidades -->

    <!-- Los cuatro elementos a continuación definen los puntos de la forma. -->
    <corners android:topRightRadius="10dp"
        android:bottomLeftRadius="10dp"
        android:topLeftRadius="0dp"
        android:bottomRightRadius="0dp"/>
</shape>
